# [gcode_macro G32]
# description: Homes all axes and performs z tilt if not already completed
# gcode:
# 	HOME_ALL

[gcode_macro _BED]
variable_size_x: 300
variable_size_y: 300
variable_origin_x: 0
variable_origin_y: 0
gcode:

[gcode_macro _CG28]
gcode:
	{% if "xyz" not in printer.toolhead.homed_axes %}
		G28
	{% endif %}

# Fluidd park clear of rear docks (matches PARK / y_safe)
[gcode_macro _CLIENT_VARIABLE]
variable_use_custom_pos: True
variable_custom_park_x: 150.0
variable_custom_park_y: 290.0
variable_custom_park_dz: 10.0
variable_retract: 1.0
variable_cancel_retract: 5.0
variable_speed_retract: 35.0
variable_unretract: 1.0
variable_speed_unretract: 35.0
variable_speed_hop: 10.0
variable_speed_move: 100.0
variable_park_at_cancel: False
variable_use_fw_retract: False
gcode:

[gcode_macro M109]
rename_existing: M99109
gcode:
	#Parameters
	{% set s = params.S|float %}
	{% set t = params.T|default(0)|int %}
	{% set heater = 'extruder' ~ (t if t > 0 else '') %}
	
	M104 {% for p in params %}{'%s%s' % (p, params[p])}{% endfor %}  ; Set hotend temp
	{% if s != 0 %}
		TEMPERATURE_WAIT SENSOR={heater} MINIMUM={s} #MAXIMUM={s+1}   ; Wait for selected hotend temp
	{% endif %}

[gcode_macro M190]
rename_existing: M99190
gcode:
	#Parameters
	{% set s = params.S|float %}

	M140 {% for p in params %}{'%s%s' % (p, params[p])}{% endfor %}   ; Set bed temp
	{% if s != 0 %}
		TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={s} #MAXIMUM={s+1}  ; Wait for bed temp (within 1 degree)
	{% endif %}

[gcode_macro DUMP_VARIABLES]
gcode:
	{% set filter_name = params.NAME|default('')|string|lower %}
	{% set filter_value = params.VALUE|default('')|string|lower %}
	{% set show_cfg = params.SHOW_CFG|default(0)|int %}
	
	{% set out = [] %}

	{% for key1 in printer %}
		{% for key2 in printer[key1] %}
			{% if (show_cfg or not (key1|lower == 'configfile' and key2|lower in ['config', 'settings'])) and (filter_name in key1|lower or filter_name in key2|lower) and filter_value in printer[key1][key2]|string|lower %}
				{% set dummy = out.append("printer['%s'].%s = %s" % (key1, key2, printer[key1][key2])) %}
			{% endif %}
		{% else %}
			{% if filter_name in key1|lower and filter_value in printer[key1]|string|lower %}
				{% set dummy = out.append("printer['%s'] = %s" % (key1, printer[key1])) %}
			{% endif %}
		{% endfor %}
	{% endfor %}
	
	{action_respond_info(out|join("\n"))}

[gcode_macro SET_ALL_HEATER_TEMPERATURE]
description: Set all hotend heaters to the same temperature and wait for them to reach it
gcode:
	{% set max_tool = printer.medusahc.tool_count|int %}
	{% set s = params.S|default(220)|float %}
	
	{% for t in range(max_tool) %}
		M104 S{s} T{t}
	{% endfor %}

	{% if s != 0 %}
		{% for t in range(max_tool) %}
			TEMPERATURE_WAIT SENSOR=extruder{% if t > 0 %}{t}{% endif %} MINIMUM={s} #MAXIMUM={s+1}
		{% endfor %}
	{% endif %}