# modified version of https://github.com/kyleisah/Klipper-Adaptive-Meshing-Purging/tree/b0dad8ec9ee31cb644b94e39d4b8a8fb9d6c9ba0/Configuration

[include macros/kamp/adaptive-mesh.cfg]
[include macros/kamp/line-purge.cfg]

[gcode_macro _KAMP_Settings]
variable_verbose_enable: False

# adaptive mesh
variable_mesh_margin: 0 # Expands the mesh size in millimeters if desired. Leave at 0 to disable.
variable_fuzz_amount: 0 # Slightly randomizes mesh points to spread out wear from nozzle-based probes. Leave at 0 to disable.

# dockable probe
variable_probe_dock_enable: False # Set to True to enable the usage of a dockable probe.
variable_attach_macro: "Attach_Probe" # The macro that is used to attach the probe.
variable_detach_macro: "Dock_Probe" # The macro that is used to store the probe.

# adaptive purge
variable_purge_height: 0.8 # Z position of nozzle during purge, default is 0.8.
variable_tip_distance: 10 # Distance between tip of filament and nozzle before purge. Should be similar to PRINT_END final retract amount.
variable_purge_margin: 10 # Distance the purge will be in front of the print area, default is 10.
variable_purge_amount: 30 # Amount of filament to be purged prior to printing.
variable_flow_rate: 12 # Flow rate of purge in mm3/s. Default is 12.

gcode:
	{action_respond_info("NOP")}